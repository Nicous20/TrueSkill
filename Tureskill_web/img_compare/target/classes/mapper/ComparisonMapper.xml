<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.ComparisonMapper">
    <select id="add" resultType="Dao.Comparison">
        insert into Comparison values (#{comparison.Attr},#{comparison.Item1},#{comparison.Item2},#{comparison.Res})
    </select>

</mapper>